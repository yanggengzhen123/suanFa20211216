;(() => {
  // 4.15 三数之和
  const nums: number[] = [-1, 0, 1, 2, -1, -4]
  function threeSum(nums: number[]): number[][] {
    const result: number[] = []
    // 从小到大排序(升序)
    nums.sort((a, b) => {
      return a - b
    })
  }
})()
let aa = 
